# Set jupyter notebook kernels

Install virtual environment and install wanted dependencies.

```
pip install ipykernel

ipykernel install --user --name brstcncrenv --display-name "Python (brstcncrenv)"
```